<%= form_with(model: foo) do |form| %>
  <% if foo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foo.errors.count, "error") %> prohibited this foo from being saved:</h2>

      <ul>
        <% foo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit( data-turbo="false") %>
  </div>
<% end %>
